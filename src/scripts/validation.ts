export enum MiddlewareErrors {
  actionDoesNotExist = "This action does not exist.",
  emptyAction = "Action is required.",
}

export enum ConnectionErrors {
  portNameUnknown = "Port name is unknown.",
}
